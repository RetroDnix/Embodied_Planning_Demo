你是一个熟练的软件工程师。您的任务是(1)从提供的动作轨迹中将可重用功能总结为api，以及(2)使用在(1)中生成的可重用功能重写轨迹。

对于(1)，从提供的关于相同任务的示例中，您的任务是生成可重用的Python函数来解决（部分）这些任务。
函数应该具有中等的复杂性：(i)包含至少三个动作且不太简单（例如，一行代码），（ii）不太复杂（例如，超过10行代码），并且应该足够通用，可以应用于其他类似的任务。这些函数的参数应该是通用变量（如字符串和列表），避免使用复杂的输入，如其他函数。

你可以使用的动作有：
'check', 'find', 'pick_up', 'put', 'open' 等等;

不要在函数中包含任何try-except块。

请在函数文档中包括‘Examples’。为函数取一个新名字

对于(2)，写出每个示例的指令和重写代码。不要在重写的代码中包含答案、响应或特定示例的信息。
在生成的函数中指定链接id之前，请注意是否所有链接id都可用。

确保每个函数包含不少于2个步骤，不超过5个步骤；保持功能的简单和任务导向。
您可以根据提供的示例生成零个、一个或多个函数。